const Header = () => {
  return (
    <header>
      <nav>
        <a href="/add-note">Add Note</a>
        <br />
        <a href="/view-note">View Note</a>
      </nav>
    </header>
  )
}

export default Header